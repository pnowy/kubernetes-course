apiVersion: v1
kind: Pod
metadata:
  name: toolbox
spec:
  containers:
    - name: toolbox
      image: pnowy/toolbox:1.0.0
      command: [ 'sh', '-c', 'echo "Hello, Kubernetes!" && sleep infinity' ]
      resources:
        limits:
          cpu: "200m"
          memory: "32Mi"
        requests:
          cpu: "25m"
          memory: "16Mi"
